**Title:** Домашнє завдання

**Source:** [https://www.edu.goit.global/uk/learn/20617573/32217374/38911114/homework](https://www.edu.goit.global/uk/learn/20617573/32217374/38911114/homework)

---

**Домашнє завдання до теми «Вивчення Helm»**

_Вітаємо! Як настрiй?_

_Сподiваємось, що ви з нетерпiнням очікуєте на новий челендж._

_Тож ви готові підняти власний кластер Kubernetes і налаштувати автоматичне масштабування для свого застосунку? 😉_

Цього разу завдання пов'язане з практичним використанням Terraform, Docker і Kubernetes.

Діючи за підказками, ви зможете побудувати функціональну інфраструктуру, готову до реального навантаження.

Цей проєкт стане чудовим доповненням до вашого портфоліо та дозволить закріпити ключові знання! 🚀

**Опис завдання**

Ваше завдання — створити кластер Kubernetes у тій самій мережі (VPC), яку ви налаштували в попередньому домашньому завданні - /Users/volodymyr/projects/go-it/dev-ops-2/goit-devops-hw5 , та реалізувати такі компоненти:

**1\. Створення кластера Kubernetes** через Terraform.

**2\. Налаштування Elastic Container Registry (ECR)** для зберігання Docker-образу вашого Django-застосунку.

**3\. Завантаження Docker-образу Django** до ECR. робли в дз4 /Users/volodymyr/projects/go-it/dev-ops-2/goit-devops-hw

**4\. Створення helm chart** (`deployment.yaml`, `service.yaml`, `hpa.yaml`, `configmap.yaml`)

**5\. Перенесення змінних середовища (env)** з теми 4 в ConfigMap, який буде використаний вашим застосунком.

**Кроки виконання завдання**

**1\. Створіть кластер Kubernetes**

-   Використовуючи Terraform, створіть кластер Kubernetes у вже існуючій мережі (VPC).
-   Забезпечте доступ до кластера за допомогою `kubectl`.

**2\. Налаштуйте ECR**

-   Використовуючи Terraform, створіть репозиторій в Amazon Elastic Container Registry (ECR).
-   Завантажте Docker-образ Django, який ви створювали в темі 4: /Users/volodymyr/projects/go-it/dev-ops-2/goit-devops-hw , до ECR, використовуючи AWS CLI.

**3\. Створіть helm. У Helm-чарті має бути реалізовано:**

-   **Deployment** — з образом Django з ECR та підключенням `ConfigMap` (через `envFrom`).
-   **Service** — типу `LoadBalancer` для зовнішнього доступу.
-   **HPA (Horizontal Pod Autoscaler)** — масштабування подів від 2 до 6 при навантаженні > 70%.
-   **ConfigMap** — для змінних середовища (перенесених із теми 4).
-   **values.yaml** — з параметрами образу, сервісу, конфігурації та autoscaler.

**Структура проєкту**

lesson-7/
│
├── main.tf                  
├── backend.tf               
├── outputs.tf               
│
├── modules/                 
│   ├── s3-backend/          
│   │   ├── s3.tf            
│   │   ├── dynamodb.tf      
│   │   ├── variables.tf     
│   │   └── outputs.tf       
│   │
│   ├── vpc/                 
│   │   ├── vpc.tf           
│   │   ├── routes.tf        
│   │   ├── variables.tf     
│   │   └── outputs.tf  
│   ├── ecr/                 
│   │   ├── ecr.tf           
│   │   ├── variables.tf     
│   │   └── outputs.tf       
│   │
│   ├── eks/                 
│   │   ├── eks.tf           
│   │   ├── variables.tf     
│   │   └── outputs.tf       
│
├── charts/
│   └── django-app/
│       ├── templates/
│       │   ├── deployment.yaml
│       │   ├── service.yaml
│       │   ├── configmap.yaml
│       │   └── hpa.yaml
│       ├── Chart.yaml
│       └── values.yaml     

**Бонус-завдання: для тих, хто має власний домен**

маю домен mx.od.ua на claudflare - пыдкажеш що робити

❗_За це завдання НЕ передбачені додаткові бали, проте ви зможете отримати фідбек від ментора та відпрацювати практичні навички._

Якщо ви купили домен (або маєте доступ до нього), можете додати в Helm-чарт підтримку **Ingress + TLS** як **бонусне завдання**:

**Задача:**

**1\. Додайте Ingress-ресурс** до Helm-чарта, який буде вмикатись через `values.yaml`:

-   Можливість задання домену (`host`)
-   Підтримка HTTPS (`tls: true`)
-   Вказівка `ingressClassName` (наприклад, `nginx`)

**2\. Підтримка cert-manager**:

-   Додайте анотацію `cert-manager.io/cluster-issuer: letsencrypt-prod` (або test).
-   Очікується, що студент самостійно встановить `cert-manager` через Helm (можна за готовою документацією або з минулих занять).

Приклад параметрів у `values.yaml`:

ingress:
  enabled: true
  className: nginx
  host: yourdomain.com
  path: /
  pathType: Prefix
  tls: true

**Результати виконання**

-   У вашому AWS-акаунті створено кластер Kubernetes.
-   ECR містить завантажений Docker-образ Django-застосунку.
-   Застосунок розгорнутий у кластері за допомогою Helm-чарта.
-   Service забезпечує доступ до застосунку через публічну IP-адресу.
-   ConfigMap підключено до застосунку через Helm.
-   HPA динамічно масштабує кількість подів.
-   _(Бонус)_ Якщо є домен, налаштовано Ingress із TLS через cert-manager.

**Критерії прийняття завдання**

 📌**Критерії прийняття домашнього завдання є обов’язковою умовою розгляду завдання ментором.** **Якщо якийсь із критеріїв не виконано, ментор надішле ДЗ на доопрацювання без оцінювання.** Якщо вам «тільки уточнити»😉 або ви застопорилися на якомусь з етапів виконання — звертайтеся до ментора у Slack).

1\. Кластер Kubernetes створений через Terraform і працює.

2\. ECR створений і містить завантажений Docker-образ.

3\. Deployment, Service і HPA створені та працюють у кластері за допомогою helm.

4\. ConfigMap створено та використовується застосунком.

5\. Проєкт запушено у GitHub-репозиторій у гілку `lesson-7` з документацією в `README.md`.

**Підготовка та завантаження домашнього завдання**

1\. Виконайте завдання та відправте його у свій репозиторій, створений раніше, у гілку lesson-7.

2\. Завантажте робочі файли на свій комп’ютер і прикріпіть їх в LMS у форматі `zip`. Назва архіву повинна бути у форматі ДЗ7\__ПІБ_.

3\. Прикріпіть посилання на гілку lesson-7 свого репозиторію та відправте на перевірку.

**Формат здачі**

1\. Посилання на ваш GitHub-репозиторій із гілкою `lesson-7`.


**Формат оцінювання**

Оцінка від **0 до 100 балів**.

-   Створення модуля `eks` — 30 балів
-   Створення модуля `ecr` — 20 балів
-   **Helm-чарт** реалізує Deployment, Service, ConfigMap, HPA — 40 балів
-   **README.md**. Оформлення документації та коректний опис команд — 10 балів

